spring.application.name=personalfinancetracker

# Server Configuration (Railway assigns PORT environment variable)
server.port=${PORT:8081}

# Database Configuration (Environment Variables for Railway)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-icy-river-ad9edq2t-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:neondb_owner}
spring.datasource.password=${DATABASE_PASSWORD:npg_TRH1qIvk2NhX}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_DDL_AUTO:update}
spring.jpa.properties.hibernate.format_sql=true

# CORS Configuration for Railway deployment
spring.web.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000,https://*.railway.app,https://*.up.railway.app}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# Logging Configuration
logging.level.org.springframework.web=${LOG_LEVEL:INFO}
logging.level.com.demo.personalfinancetracker=${LOG_LEVEL:INFO}
